/** Max size per image file for admin uploads (bytes). */
export const MAX_ADMIN_IMAGE_UPLOAD_BYTES = 100 * 1024 * 1024;

/** Max combined size for a single multi-file upload request (product gallery). */
export const MAX_ADMIN_IMAGE_BATCH_TOTAL_BYTES = 500 * 1024 * 1024;
